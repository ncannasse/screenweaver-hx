
class Flash {

    static var enabledCom = swhx.Connection;
    
    static function main() {
		trace(
			"Run this sample from console to see\n"+
			"window messages being printed,\n"+
			"on Windows systems, on Right Click."
		);	
    }   
}
